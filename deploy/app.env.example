# Application environment variables
# These are passed directly to the Ktor server container

# School catalog settings
SCHOOL_CATALOG_ENDPOINT=https://overpass-api.de/api/interpreter

# Note: Database settings are configured via docker-compose environment
# and the .env file - no need to set them here
